{% for item in projects %}
Host {{ item.project_name | default(item.name) }}.{{ item.url.split(':').0  }}
HostName {{ item.url.split(':').0 }}
User git
IdentityFile {{ talend_user_home_dir }}/.ssh/id_rsa_{{ item.name }}

{% endfor %}
