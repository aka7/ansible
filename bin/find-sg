#!/bin/bash
function usage
{
    echo "$0 <aws_account> <instance_id>"
    exit 1;
}
[ -z "$1" ] && usage
[ -z "$2" ] && usage
aws-vault exec $1 -- aws ec2 describe-instances --instance-ids $2 --query "Reservations[].Instances[].SecurityGroups[].GroupId[]" --output text
