#!/bin/bash
#
function usage
{
    echo "$0 <aws_account> <instance_id>"
    exit 1;
}
[ -z "$1" ] && usage
[ -z "$2" ] && usage
aws-vault exec $1 -- aws autoscaling describe-auto-scaling-instances --instance-ids="$2"
