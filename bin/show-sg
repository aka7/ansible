#!/bin/bash
#
function usage
{
    echo "$0 <aws_account> <sgroup1 sgroup2..>"
    exit 1;
}
[ -z "$1" ] && usage
[ -z "$2" ] && usage
aws-vault exec $1 -- aws ec2 describe-security-groups --group-ids $2
