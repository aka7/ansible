#!/bin/bash
function usage
{
    echo "$0 <aws_account>"
    exit 1;
}
[ -z "$1" ] &&  usage

aws-vault exec $1 -- aws ec2 --output table --query 'Vpcs[*].{OwnerId:OwnerId,VpcId:VpcId,Name:Tags[?Key==`Name`].Value|[0],CidrBlock:CidrBlock}' describe-vpcs
