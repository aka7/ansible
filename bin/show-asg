#!/bin/bash
function usage
{
    echo "$0 <aws_account> <asg_name>"
    exit 1;
}
[ -z "$1" ] && usage
[ -z "$2" ] && usage
aws-vault exec $1 -- aws autoscaling describe-auto-scaling-groups --auto-scaling-group-name $2
